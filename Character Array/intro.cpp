#include <iostream>
#include <string.h>
using namespace std;

int main(){

    // char name[100];
    // cin >> name[2];

    // cout << name;

    // char arr[100];
    // // cin >> arr;
    // cin.getline(arr, 50);       //to print space , by default stops at enter

    // cout << arr;


    char arr[50];
    cin>> arr[2];
    cin >> arr[49];

    cin>>arr;
    cin.getline(arr, 50);

    return 0;
}